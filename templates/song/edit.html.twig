{% extends 'base.html.twig' %}

{% block title %}Edit {{ song.name }}{% endblock %}

{% block body %}
    <h1>{{ song.name }}</h1>

    {{ include('song/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_song_index') }}">back to list</a>

    <a href="{{ path('app_homepage') }}">Accueil</a>

    {{ include('song/_delete_form.html.twig') }}
{% endblock %}
